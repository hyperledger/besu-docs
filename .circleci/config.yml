---
version: 2.1

jobs:
  dummy:
    docker:
      - image: cimg/base:2022.06
    steps:
      - run:
          name: dummy
          command: echo "dummy step to prevent Circle from failing"

workflows:
  version: 2
  default:
    jobs:
      - dummy
